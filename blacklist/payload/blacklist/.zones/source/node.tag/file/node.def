type: txt
help: A conditional DNS forwarding source file with a list of zone names

val_help: http; Example: /config/user-data/zones.txt
comp_help: The file must contain valid domain names, one domain or host name per line

end: if [[ "$COMMIT_ACTION" != "DELETE" ]]; then
        if [[ ! -f "$VAR(./@@)" ]]; then
            exit 1;
        fi;
    fi
