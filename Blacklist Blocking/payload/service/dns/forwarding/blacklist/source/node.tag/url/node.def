type: txt
help: Add blacklist source url here e.g.
                http://someonewhocares.org/hosts/zero/
comp_help: Make sure the matching regex search only returns the FQDN or hostname.

commit:expression: $VAR(../regex) != ""; "A source regex search string must be set" 
